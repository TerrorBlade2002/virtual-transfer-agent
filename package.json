{
  "name": "retell-vta-webhook",
  "version": "3.0.0",
  "description": "Retell VTA Webhook — Verification Handshake + Disposition Sync + Campaign Upload",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "csv-parser": "^3.0.0",
    "xlsx": "^0.18.5",
    "multer": "^1.4.5-lts.1"
  },
  "engines": {
    "node": ">=18"
  }
}
